NEXT_PUBLIC_API_URL=http://localhost:8000
BACKEND_INTERNAL_URL=http://localhost:8000

# Shared-secret token — must match the backend DMS_API_TOKEN when the gate is on.
# Baked into the client bundle at build time; leave empty when the gate is off.
NEXT_PUBLIC_DMS_API_TOKEN=
