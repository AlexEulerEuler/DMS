# DMS Console Contracts

API contracts and generated schema artifacts for the DMS console.

The backend OpenAPI document should be generated into:

```text
apps/dms-console/contracts/openapi/backend.json
```

Generate it with:

```bash
npm run openapi:dms:backend
```
